import '@todo/styles/globals.scss'

// import { Ubuntu } from '@next/font/google'

// const ubuntu = Ubuntu({
//   subsets: ['latin'],
//   weights: ["400", "700"]
// })


export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}
